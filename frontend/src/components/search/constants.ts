// Enums matching backend DTO
export enum RatingFilter {
  ALL = 'ALL',
  SAFE = 'SAFE',
  R18 = 'R18',
}

export enum SortOption {
  NEWEST = 'newest',
  POPULAR = 'popular',
}

// Popular tags for quick filtering
export const POPULAR_TAGS = ['genshin', 'anime', 'fantasy', 'portrait', 'landscape', 'furry', 'fanart'];
